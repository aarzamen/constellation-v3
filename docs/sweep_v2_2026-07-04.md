# SWEEP v2 — fresh exports + Desktop consolidation (2026-07-04)

Authority: Mike, 2026-07-04. Executor: sole Constellation session. Discipline
held: **archive first, originals immutable, ONE incremental embed, notes
invariant asserted**. No deletions; no existing chunk re-embedded.

## Result

| | conversations | chunks | index size | notes.json sha |
|---|---|---|---|---|
| before | 2,936 | 19,715 | 127.2 MB | `5d45a577…b8a770` |
| after  | **3,665** (+729) | **25,753** (+6,038) | 179.2 MB | `5d45a577…b8a770` (unchanged) |

Per-provider delta (all additive, delta by conversation id):

| provider | before | after | Δ | notes |
|---|---|---|---|---|
| claude | 1,278 | 1,293 | **+15** | fresh Anthropic export; new convs 2026-07-02 → 07-04 |
| grok | 0 | 201 | **+201** | **NEW PROVIDER** (xAI); 2025-02-18 → 2026-05-21 |
| abacus | 0 | 512 | **+512** | **NEW PROVIDER** (ChatLLM Teams); 2025-01-08 → 2026-07-04 |
| gemini | 275 | 276 | **+1** | one new activity entry past the last-indexed cutoff |
| chatgpt | 1,150 | 1,150 | 0 | already fully ingested (2026-07-03 export) |
| claude-code | 51 | 51 | 0 | the 20 Desktop `local_` convs already indexed |
| notebooklm | 11 | 11 | 0 | — |
| aistudio | 171 | 171 | 0 | — |

Test suite: **197 passed, 1 skipped** (before and after embed). notes.json
byte-identical across the embed (invariant sha `5d45a5779e1e04b52f128a6d481dca6356dcbcfb26281875b8ecb9c479b8a770`).

## Phase 0 — fingerprint (which sources proved novel vs duplicate)

| Downloads file | resolved as | provider | verdict |
|---|---|---|---|
| `data-ba821840-…-batch-0000.zip` (78 MB → 310 MB) | Anthropic export (`conversations.json` 1,356 convs + `projects/` + `design_chats/` + `users.json`) | claude | **NOVEL** (+15 delta) |
| `takeout-…001.zip` + `…3-001.zip` (972 MB) | Google Takeout — Gemini Apps `MyActivity.json` (1,911 entries, **JSON** format, not the prior HTML) | gemini | **near-duplicate** (+1) |
| `63760187-…zip` (55 MB) | xAI export — `prod-grok-backend.json` (204 convs) | grok | **NOVEL** (+201) |

The intended **fourth provider (ChatGPT/OpenAI) had no fresh export in this
sweep.** See action items.

## Phase 2/3 — vintage table (duplicate vs novel)

| source | provenance | disposition |
|---|---|---|
| Anthropic zip (`data-ba82…`) | fresh, 1,356 convs (1,292 non-empty) | **ingested** claude +15; canonical claude source |
| xAI zip (`63760187…`) | fresh, 204 convs (3 empty dropped) | **ingested** grok +201 via `grok_to_anthropic.py` → `grok_parser` |
| Gemini `MyActivity.json` | UTC-ms JSON; boundary entry `2026-07-04T05:25:01.603Z` already indexed under old HTML wall-clock id | **ingested** gemini +1 (temporal cutoff dedupe) |
| Abacus `…export-2.json` (62 MB) | canonical, 557 convs (512 with text) | **ingested** abacus +512 (new `core/abacus_parser.py`) |
| Abacus `…export.json` (1.8 MB) | small variant | archived; not canonical |
| Desktop `gpt chats/conversations.json` (303 MB, 1,341) | **mislabeled Claude export** — 1,341/1,341 uuids match the Anthropic zip; 1,278 already in corpus | **NOT ingested** (subsumed by claude delta) |
| `gpt chats/projects/` (31), `design_chats/` (19) | **byte-identical** to Anthropic zip | duplicate; already archived within the zip |
| `gpt chats/memories.json`, `users.json`, `audio/` (221 GPT-4 WAVs) | genuine OpenAI assets | **archived** to `ground-truth/openai/2026-07-04/`; not ingested |
| `gpt chats/older exports/from gemini apps folder/` twins (`70b2e0…`, `e2a93f…`) | sha `08348ace…` **matches** `ground-truth/openai/2025-08-24` | duplicate; no re-archive, no deletion |
| `gpt chats/e148ef…-2026-07-02…/` (13 shard `conversations-0NN.json`) | same shard set as `ground-truth/openai/2026-07-03` | duplicate; already ingested (chatgpt=1,150) |
| `claude hx/` markdown parts 1–3 (`e96bebee…`, ~1,005 `## `+`*Date:*` blocks) | human-readable markdown; dates messy (incl. bogus `2043`) | **archived** to `ground-truth/anthropic/2026-07-04/claude-hx-markdown/`; **not ingested** (redundant with canonical zip; no uuids to dedupe) |
| `claude hx/from gemini apps folder/` `conversations.json` (20 `local_` convs) + `Claude Code chats-….zip` (same 20) | Claude Code sessions (`<command-message>…`, `@/…/SPRINT.md`) | all 20 already in corpus → **0 new** |

## memories.json inventory (NEW DATA CLASS — not ingested)

`ground-truth/openai/2026-07-04/memories.json`: a 1-record JSON list with keys
`conversations_memory` (str, **9,484 chars** — the model-curated running memory
of Mike), `project_memories` (dict, **19 projects**), `account_uuid` (str). This
is provider-curated *memory about the user*, not a conversation — a distinct data
class. **Held pending an explicit design decision** (per Mike). Candidate
handling: treat as a single high-salience "profile" document, or split
`project_memories` per-project; either way a new schema, not the conversation
pipeline.

## Human action items

1. **ChatGPT — the missing fourth provider.** No fresh OpenAI conversation
   export arrived in this sweep. `gpt chats/conversations.json` was a misfiled
   Claude export. corpus chatgpt is current only through the **2026-07-03**
   export (1,150). If newer ChatGPT chats exist, pull a fresh Data Export and
   drop `conversations-*.json` for a delta ingest.
2. **memories.json design decision** (above) before any ingest of ChatGPT
   memory / `project_memories`.
3. **Audio lane** (roadmap §7): 221 GPT-4 voice WAVs (153 MB) archived at
   `ground-truth/openai/2026-07-04/audio/`, joining the earlier voice-shell
   class. Local ASR (Whisper/Parakeet) would turn these into embeddable text
   with no model migration.
4. **Grok drop count:** 3 of 204 Grok conversations were empty after
   normalization (0 text-bearing messages) → 201 ingested. Expected (metadata-
   only threads).
5. **Gemini cutoff caveat:** the JSON delta is strictly *temporal* (entries
   newer than the last-indexed activity). If the new JSON and the prior HTML
   export diverged inside the overlap window, a handful of mid-window entries
   would be neither in the 275 nor re-added. Consistent with "delta-ingest new
   activity"; flagged for transparency.

## Provenance / new code

- `core/abacus_parser.py` — `parse_abacus_export`, provider `abacus`
  (registered in `core/provider_registry.py`).
- `core/gemini_activity_parser.py` — added `extract_entries_json()` for the
  Takeout JSON serialization (UTC timestamps; `Prompted`/`Said` prefix strip).
- `scripts/sweep_ingest.py` — one-shot orchestrator; reuses
  `nightly_ingest.incremental_embed` (vector-reuse + notes invariant).
- `tests/test_sweep_parsers.py` — 3 tests (abacus roles/skip, gemini JSON
  extract+sessionize, cutoff filtering).
- Archives (all under gitignored `ground-truth/`):
  `anthropic/2026-07-04/`, `google/2026-07-04/` (takeout zips),
  `xai/2026-07-04/`, `abacus/2026-07-04/`, `openai/2026-07-04/` (assets).

**Untouched, per instruction:** `_to review/`, `Abacus game history.`
